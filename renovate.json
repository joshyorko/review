{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "local>projectbluefin/renovate-config"
  ],
  "customManagers": [
    {
      "description": "Track hivecommons/hive origin/v4 as a pinned commit SHA. The SHA is duplicated in the launcher, the image, and the README, and tests/image-contract.sh plus validate.yml require all three to agree, so one depName keeps every match in a single Renovate branch and PR.",
      "customType": "regex",
      "managerFilePatterns": [
        "/^justfile$/",
        "/^image\\/Containerfile$/",
        "/^README\\.md$/"
      ],
      "matchStrings": [
        "hive_commit := \"(?<currentDigest>[0-9a-f]{40})\"",
        "ARG HIVE_COMMIT=(?<currentDigest>[0-9a-f]{40})",
        "layers the pinned Hive runtime at `(?<currentDigest>[0-9a-f]{40})`"
      ],
      "currentValueTemplate": "v4",
      "depNameTemplate": "hivecommons/hive",
      "packageNameTemplate": "https://github.com/hivecommons/hive",
      "datasourceTemplate": "git-refs"
    }
  ],
  "packageRules": [
    {
      "description": "Restrict automerge to immutable-ref housekeeping (pin and pinDigest). Other update types require review gate (#499).",
      "matchUpdateTypes": [
        "pin",
        "pinDigest"
      ],
      "automerge": true,
      "automergeType": "pr",
      "automergeStrategy": "squash",
      "platformAutomerge": true
    },
    {
      "description": "Keep the FSDK lab-runner base image digest in image/Containerfile current",
      "matchManagers": [
        "dockerfile"
      ],
      "matchPackageNames": [
        "ghcr.io/projectbluefin/lab-runner"
      ],
      "pinDigests": true
    },
    {
      "description": "Group OMP (can1357/oh-my-pi) updates across containerfiles into a single PR and build to validate",
      "matchPackageNames": [
        "can1357/oh-my-pi"
      ],
      "groupName": "can1357/oh-my-pi",
      "automerge": true,
      "automergeType": "pr",
      "automergeStrategy": "squash"
    }
  ]
}
